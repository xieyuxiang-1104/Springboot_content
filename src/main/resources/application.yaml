spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/liuyan?useUnicode=true&amp;characterEncoding=utf-8
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
  resources:
    static-locations: classpath:templates/
server:
  port: 8080

mybatis:
  mapper-locations: classpath:/mapper/**.xml
  type-aliases-package: xie.dao